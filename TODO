theme
    add themed components e.g., ThemedView whose bgColor is fetched from a theme hook
    add themed classes e.g., dark:bg-zinc-900
    add global theme context
    make sure to use a json file for all theme colors
    add a theme toggle button in the settings screen

do something about the small delay when doing video item operations like entering the page, invoking the properties modal
    this is caused by sql querying so it's a given